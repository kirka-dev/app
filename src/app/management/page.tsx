import styles from "@styles/Management.module.scss";
import cn from "classnames";

const Management = () => {
  return (
    <div className={cn(styles.Management)}>
      Вывод статистики
    </div>
  )
}

export default Management
